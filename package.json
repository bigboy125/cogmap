{
  "name": "cogmap",
  "version": "0.1.0",
  "private": true,
  "description": "CogMap — AI 协作工程化方法论开放协议 (monorepo)",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "node --test packages/core/test/smoke.test.mjs",
    "lint": "echo 'no linter yet'",
    "release:core": "cd packages/core && npx --no-install release-it",
    "release:cli": "cd packages/cli && npx --no-install release-it",
    "release:mcp": "cd packages/mcp-server && npx --no-install release-it"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/bigboy125/cogmap.git"
  },
  "keywords": [
    "ai",
    "claude",
    "coding-agent",
    "skills",
    "methodology",
    "project-memory"
  ],
  "author": "bigboy125",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "release-it": "^20.0.1"
  }
}
